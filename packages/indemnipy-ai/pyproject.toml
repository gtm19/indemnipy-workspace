[project]
name = "indemnipy-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gareth Maddock", email = "gtmaddock@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "oletools>=0.60.2",
    "openpyxl>=3.1.5",
    "pydantic-ai>=2.27.0",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "logfire[system-metrics]>=4.40.0",
    "pydantic-ai-harness[codemode]>=0.18.0",
]
